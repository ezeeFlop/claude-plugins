{
  "mcpServers": {
    "rayonne": {
      "command": "uvx",
      "args": ["rayonne-mcp@0.3.3"],
      "env": {
        "RAYONNE_API_KEY": "${user_config.api_key}",
        "RAYONNE_API_URL": "${user_config.api_url}",
        "RAYONNE_READ_ONLY": "${user_config.read_only}"
      }
    }
  }
}
